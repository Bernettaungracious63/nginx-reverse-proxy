# Save this file as .env before running docker compose up -d

# ---- GOACCESS LOG DASHBOARD ----
TZ=America/New_York #update this to your timezone and remove this comment.
LOG_TYPE=NPM
SKIP_ARCHIVED_LOGS=False
DEBUG=False
HTML_REFRESH=5
KEEP_LAST=30
PROCESSING_THREADS=1
BASIC_AUTH=True
BASIC_AUTH_USERNAME=admin #update this to your username and remove this comment.
BASIC_AUTH_PASSWORD=Password123$ #update this to your user password and remove this comment.